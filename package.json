{
  "name": "opencode-claude",
  "version": "1.0.0",
  "private": true,
  "description": "Workspace for running Claude (Max/Pro) in OpenCode: Meridian proxy + first-party plugin, with local fixes for tool-calling reliability and subagent dispatch.",
  "scripts": {
    "setup": "./scripts/install-local.sh",
    "setup:test": "./scripts/install-local.sh --test"
  },
  "license": "MIT"
}
